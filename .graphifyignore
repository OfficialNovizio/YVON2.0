# Graphify Ignore — controls what graphify scans
# Exclude everything that is NOT app code.
# Goal: only scan app/, lib/, src/ for meaningful architecture graphs.

# Agent documentation (markdown + skill templates — not app code)
agent-department/
agents/
brands/
brand-context/
reference/
.yvon-os/
memory/
project-memory/

# Graph output (circular reference if scanned)
graphify-out/

# Shell + PowerShell scripts (appear as singleton noise communities)
scripts/

# Supabase migrations (SQL — not JS/TS app logic)
supabase/

# Tests (create false dependency edges into lib/)
__tests__/

# Static assets + fonts (no extractable architecture)
public/
Fonts/
visual-companion/

# Config noise (each becomes a useless singleton community)
next-env.d.ts
next.config.ts
postcss.config.mjs
tailwind.config.ts
tsconfig.json
tsconfig.tsbuildinfo
vercel.json
.eslintrc.json

# Python automation scripts (wrong language, not app logic)
*.py

# All markdown files
*.md

# Lock files
package-lock.json
